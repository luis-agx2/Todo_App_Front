import { Menu, MenuItem } from '../interfaces/menu-item.interface';

const MENU_USER: MenuItem[] = [{ id: 'home', icon: 'home', path: '/', name: 'Home' }];

export const MENU: Menu = {
  USER: MENU_USER
};
